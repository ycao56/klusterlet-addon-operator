apiVersion: apps/v1
kind: Deployment
metadata:
  name: klusterlet-component-operator
  namespace: klusterlet
spec:
  template:
    spec:
      imagePullSecrets:
      - name: multicloud-image-pull-secret
         