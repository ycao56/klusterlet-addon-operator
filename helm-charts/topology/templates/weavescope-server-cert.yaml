{{- if .Values.enabled }}
apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: {{ template "topology.fullname" . }}-weavescope-server-cert
  labels:
    app: {{ template "topology.name" . }}
    chart: {{ template "topology.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  secretName: {{ .Release.Name }}-weavescope-server-secret
  issuerRef:
    name: {{ .Release.Name }}-klusterlet-cert-issuer
    kind: Issuer
  commonName: weavescope-server-cert
  organization:
  - IBM
  dnsNames: null
{{- end }}
